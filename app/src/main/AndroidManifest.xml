<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="to.pta.ha">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HA">
        <activity android:name="to.pta.ha.ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="to.pta.ha.ui.questions.ManageQuestionsActivity">
        </activity>
        <activity android:name="to.pta.ha.ui.visualization.StatsActivity">
        </activity>
        <activity android:name="to.pta.ha.ui.SettingsActivity">
        </activity>
    </application>

</manifest>